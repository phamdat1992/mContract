const config = {
    destDir: './dest',
    srcDir: './src'
};

// sources config
config.jsSrcDir = config.srcDir + '/js';

// destination config
config.jsDestDir = config.destDir + '/js';

module.exports = config;