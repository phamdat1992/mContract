package vn.inspiron.mcontract.modules.Contract.model;

import java.util.HashMap;
import java.util.Map;

public enum ContractStatus {
   /* WAITING_FOR_APPROVAL(1),
    WAITING_FOR_SIGNATURE(2),
    SIGNED(3),
    APPROVED(4),
    CANCELLED(5);

    private int value;
    private static Map map = new HashMap<>();

    ContractStatus(int value) {
        this.value = value;
    }

    static {
        for (ContractStatus status : ContractStatus.values()) {
            ContractStatus.map.put(status.value, status);
        }
    }

    public static ContractStatus valueOf(int contractStatus) {
        return (ContractStatus)ContractStatus.map.get(contractStatus);
    }

    public int getValue() {
        return value;
    }*/
}
